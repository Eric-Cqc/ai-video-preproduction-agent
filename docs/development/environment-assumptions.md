# 环境假设

当前仓库是文档优先的 foundation bootstrap；不假设 Next.js、FastAPI、Node 包管理器、Python 环境、数据库或云账号存在。`.node-version` 和脚本目录是已有仓库内容，非本阶段技术选型或安装指令。

## 冻结决定

本阶段不得通过“准备环境”引入运行时、依赖、应用配置或外部服务。

## 可替换假设与复审触发

未来语言、框架、数据库、队列和部署环境均可替换。进入实现阶段且产品契约、威胁建模和首批验收场景获批准后，新增 ADR 与可复现的环境说明。
