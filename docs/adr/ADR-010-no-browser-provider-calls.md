# ADR-010：禁止浏览器直连 Provider

- 状态：已接受（冻结）
- 决定：浏览器只调用本系统受控 API；模型、存储、身份及其他 Provider 调用由服务端或受限 Worker 发起。
- 原因：保护凭据、集中授权/tenant 策略、审计和成本控制。
- 后果：任何临时前端 SDK 或客户端密钥方案均不被接受；上传/下载也须使用受控、短时且最小权限的服务端授权路径。
- 可替换假设：具体传输协议和临时授权实现。
- 复审：无；此为长期安全原则。实现细节可用新 ADR 演进，但不得取消服务端策略边界。
