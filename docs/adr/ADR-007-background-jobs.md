# ADR-007：后台 Job

- 状态：已接受（冻结）
- 决定：生成建议、导入、导出及其他长时操作以可追踪、幂等、可重试的 Job 表示。
- 原因：使用户请求保持响应，并为失败、来源和成本建立可观测性。
- 后果：Job 不是领域真相来源，结果仍须写入版本化领域对象并留审计关联。
- 可替换假设：队列、调度器、重试策略具体参数。
- 复审：吞吐、可靠性或成本指标连续两个周期不达标时，以新 ADR 选择运行时。
