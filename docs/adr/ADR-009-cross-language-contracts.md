# ADR-009：跨语言契约

- 状态：已接受（冻结）
- 决定：跨 Web/API/Worker 的数据通过显式、版本化 Schema/接口契约交换，不共享内部运行时模型。
- 原因：允许语言和框架独立演进，同时保护领域语义。
- 后果：契约兼容性需要测试；破坏性变化须有迁移与弃用策略。
- 可替换假设：契约描述格式、代码生成工具和校验库。
- 复审：两个消费者的兼容性问题证明现有格式不可维护时，以新 ADR 更换描述机制。
