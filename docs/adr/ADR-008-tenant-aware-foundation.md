# ADR-008：多租户预留

- 状态：已接受（冻结）
- 决定：所有领域对象、访问判定、Job、审计和未来查询都具备 tenant 归属与隔离语义。
- 原因：避免试点后补救性隔离和数据迁移风险。
- 后果：即使初期只有单一组织，也不以“全局默认 tenant”绕过边界。
- 可替换假设：tenant 层级、身份提供方和物理隔离策略。
- 复审：共享生产、外部协作者或受监管数据出现前，完成威胁建模和隔离设计 ADR。
