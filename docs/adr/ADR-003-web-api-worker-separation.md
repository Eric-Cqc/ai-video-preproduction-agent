# ADR-003：Web、API 与 Worker 职责分离

- 状态：已接受（冻结）
- 决定：Web 负责交互；API 负责授权、领域规则和同步协调；Worker 处理长时、可重试任务。
- 原因：保护信任边界并避免长任务阻塞用户请求。
- 后果：三者可同一部署单元起步，但职责、契约与权限分离。
- 可替换假设：进程模型、队列和部署拓扑。
- 复审：任务量或延迟指标连续两个周期不达标时，评估运行时，不改变服务端信任边界。
