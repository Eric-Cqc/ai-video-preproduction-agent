# ADR-004：统一结构化视频制作 Schema

- 状态：已接受（冻结）
- 决定：简报、脚本、场景、镜头、资产、评审、批准和导出以版本化结构化 Schema 表达。
- 原因：这些对象才是可审查、可协作、可导出的业务真相；Prompt 不具备稳定语义。
- 后果：AI 输出先映射为草案 Schema；Prompt/模型参数作为来源证据关联，不可替代领域实体。
- 可替换假设：字段细节、序列化格式和迁移机制。
- 复审：已批准流程无法表达，且两次兼容迁移均不可行时，以新 ADR 更新模型。
