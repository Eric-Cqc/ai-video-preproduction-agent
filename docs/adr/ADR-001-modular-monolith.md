# ADR-001：模块化单体

- 状态：已接受（冻结）
- 决定：以单一可部署系统承载按领域划分的模块；模块仅经公开契约协作。
- 原因：当前需要快速验证统一制作流程，同时避免分布式复杂度。
- 后果：可独立演进模块边界，但不得预设服务间 RPC、分布式事务或独立部署。
- 可替换假设：单体内部语言、框架和部署方式。
- 复审：连续两个发布周期有量化的独立伸缩/故障隔离/部署阻塞证据，且边界与数据所有权明确时，以新 ADR 评估拆分。
